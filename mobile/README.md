# Mini Grocery (Flutter customer app)

Setup:

    flutter pub get
    flutter run

The API address is in `lib/config/api_config.dart`. The default works for the
Android emulator (10.0.2.2). For a physical phone use the PC LAN IP.

Test login: customer@example.com / customer123
